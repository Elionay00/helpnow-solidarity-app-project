generator client {
  provider = "prisma-client-js"
}

// COLE ESTE BLOCO AQUI
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db" // Usaremos um arquivo de banco de dados simples por enquanto
}

// Seus models User e Transaction vêm depois...
model User {
  // ...
}

model Transaction {
  // ...
}